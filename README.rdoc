After cloning , Go to folder which is now cloned.This is folder should named as Blog.                                                

Run sudo bundle install to install all gems required to run the app.
